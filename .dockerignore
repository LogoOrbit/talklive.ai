.git
.gitignore
node_modules
data

# Marketing collateral (~8MB of audio/GIF/posters) is never served by the
# server - public/ is the only static root - so keep it out of the image.
marketing

.replit
DEPLOY-FLY.md
README.md
